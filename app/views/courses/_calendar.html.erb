<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, value: @course.name, readonly: true %>
  </div>

  <div class="field">
  	<%= f.label :days %><br />
  	<%= f.number_field :days, in: @course.days..365, step: 1 %>
  </div>
  <br />

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
