= simple_form_for ([@post, @post.comments.build]) do |f| 
	= f.input :content, required: true, :input_html => { cols: 50, rows: 10}, label: false

	.form-actions
		= f.button :submit, class: "btn btn-sm btn-primary"
