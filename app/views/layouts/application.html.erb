<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <title>FluSim</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<%-#--- BEGIN NAVIGATION BAR ----%>
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <p class="navbar-brand">FluSim</p>
        </div>
        <div>
            <ul class="nav navbar-nav navbar-right">
                <li><% if current_user.present? && current_user.instructor? %>
                        <p class="navbar-text">Instructor Mode</p>
                    <% elsif current_user.present? && !current_user.instructor? %>
                        <p class="navbar-text">Student Mode</p>
                    <% end %>
                </li>
                <li><% if current_user.present? %>
                        <p class="navbar-text"><%= current_user.email %></p>
                    <% end %>
                </li>
                <li><% if current_user.present? %>
                        <%= button_to "Sign Out", destroy_user_session_path, :id => "signout-button", method: :delete %>
                    <% end %>
                </li>
            </ul>
        </div>
    </div>
</nav>
<%-#--- END NAVIGATION BAR ----%>

<%-#--- BEGIN MAIN CONTAINER ----%>
<div id="main-container" class="container">
    <div class="alertnotice">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
    </div>

    <%-#--- BEGIN FIRST ROW ----%>
    <div class="row">
        <div id="sidebar" class="col-xs-6 col-md-3">
            <h1><span class="label label-success">Influenza Simulation</span></h1>
        </div>
        <div id="main" class="col-xs-6 col-md-9">
            <%= yield %>
        </div>
    </div>
    <%-#--- END FIRST ROW ----%>
</div>
<%-#--- END MAIN CONTAINER ----%>

</body>
</html>
