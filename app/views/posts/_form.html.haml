= simple_form_for @post do |f|
	= f.input :title, required: true
	= f.input :content, required: true, :input_html => { cols: 50, rows: 10}

	.form-actions
		= f.button :submit, class: "btn btn-sm btn-primary"
